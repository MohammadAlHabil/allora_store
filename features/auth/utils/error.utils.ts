// import { AppError, ConflictError } from "@/shared/lib/errors";

// export class AuthError extends AppError {
//   constructor(message = "Authentication failed", details?: unknown) {
//     super(message, "AUTH_ERROR", 401, "authentication", details);
//   }
// }
