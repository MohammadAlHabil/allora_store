// Redux store removed — cart is now managed by React Query.
// This file remains as a deprecated stub for a transitional period.

export type RootState = unknown; // deprecated
export type AppDispatch = unknown; // deprecated

export const store = null as unknown as {
  getState: () => unknown;
  dispatch: (...args: unknown[]) => unknown;
};
